import React from "react";

function MarqueeText() {
  return (
    <div>
      var typed = new Typed(".typing-text", strings = ["Designer", "Developer"],
      typeSpeed = "200", backSpeed = "100", backDelay="100", loop = "true", );
    </div>
  );
}

export default MarqueeText;
